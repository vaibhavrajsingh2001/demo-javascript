function isEven(o) {
  (o = 2) ? console.log(`${o} is even`) : console.log(`${o} is odd`);
}
function isNumber(o) {
  let e = void 0;
  (e = o % 2) && console.log(`Number: ${e}`);
}
function isTruthy(o) {
  return Boolean(o);
}
function area(o) {
  return Math().PI * o * o;
}
function isFooAvailable(o) {
  return console.log(`Value of obj[foo]: ${o.foo}`), o.hasOwnProperty("foo");
}
function findFooBar() {
  /=foo   bar/.test("foobar");
}
function consoleFoo(o) {}
function isGreaterThan(o, e) {
  Array.isArray(o) && o.map((o) => (o > e ? arguments.callee(o - 1) * o : o));
}
function callHiEveryMinutes(o) {
  !window && o
    ? setTimeout("alert('Hi')", 1e3 * o)
    : window.setTimeout("alert('Hi')", 1e3 * o);
}
let result = isFooAvailable({ bar: "bar", z: "z" })(void 0, 0);
function checkYoda() {
  console.log("I am yoda");
}
const crypto = require("crypto");
function getEncryptedKey() {
  return crypto.createCipheriv("aes-192-ecb", Buffer.from(ENCRYPTION_KEY), iv);
}
function isMatched(o) {
  return o.match(/hasTheMagic/)[0] ? process(o) : null;
}
function commentCheck() {
  console.log("no comment");
}
